[build-system]
requires=["setuptools>=69"]
build-backend="setuptools.build_meta"
[project]
name="datainspect"
version="0.1.0"
description="Terminal data inspection helpers"
requires-python=">=3.10"
[project.scripts]
datainspect="datainspect.cli:main"
[tool.setuptools.packages.find]
where=["src"]
